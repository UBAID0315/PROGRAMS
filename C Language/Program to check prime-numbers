#include<stdio.h>
void main ()
{
    int numb,i,j,arr[9],count;
   
       for(i = 0; i < 9; i++)
       {
           printf("Enter the value %d: ",i+1);
           scanf("%d",&arr[i]);    
     }

    for ( i = arr[i]; i <= 10; i++)
    {
        for ( j = 2; j<=i; j++)
        {
           if (i%j!=0)         
           {
           continue;
           }
          else
          {
            if (i==j)
            {
               count++;        
            }
            break;
          }
        }
       }
    printf("\nTotal primes no are %d",count);
	}
